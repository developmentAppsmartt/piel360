<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/skin_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="#000000">

  <androidx.camera.view.PreviewView
    android:id="@+id/preview_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />

  <View
    android:id="@+id/oval_mask"
    android:layout_width="@dimen/skin_oval_width"
    android:layout_height="@dimen/skin_oval_height"
    android:layout_gravity="center"
    android:layout_marginTop="@dimen/skin_oval_margin_top"
    android:background="@drawable/skin_analysis_oval"
    android:alpha="0.98" />

  <!-- Guía / countdown del Camera Kit -->
  <LinearLayout
    android:id="@+id/guide_block"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingHorizontal="24dp">

    <TextView
      android:id="@+id/guide_text"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:gravity="center"
      android:shadowColor="#B0000000"
      android:shadowDx="0"
      android:shadowDy="1"
      android:shadowRadius="6"
      android:text="Inicializando…"
      android:textColor="#FFFFFF"
      android:textSize="@dimen/skin_guide_text"
      android:textStyle="bold" />

    <TextView
      android:id="@+id/countdown_text"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginTop="4dp"
      android:gravity="center"
      android:shadowColor="#B0000000"
      android:shadowDx="0"
      android:shadowDy="2"
      android:shadowRadius="8"
      android:textColor="#FFFFFF"
      android:textSize="@dimen/skin_countdown_text"
      android:textStyle="bold"
      android:visibility="gone" />
  </LinearLayout>

  <!-- Loader animado (captura / procesando) -->
  <com.piel360.app.skinanalysis.CapturePulseLoaderView
    android:id="@+id/capture_loader"
    android:layout_width="96dp"
    android:layout_height="96dp"
    android:layout_gravity="center"
    android:visibility="gone" />

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="top"
    android:orientation="vertical"
    android:paddingTop="@dimen/skin_top_padding"
    android:paddingHorizontal="16dp">

    <RelativeLayout
      android:layout_width="match_parent"
      android:layout_height="@dimen/skin_header_row_height">

      <TextView
        android:id="@+id/btn_back"
        android:layout_width="@dimen/skin_header_btn"
        android:layout_height="@dimen/skin_header_btn"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@drawable/skin_analysis_circle_btn"
        android:gravity="center"
        android:text="‹"
        android:textColor="#222222"
        android:textSize="@dimen/skin_header_btn_text"
        android:textStyle="bold" />

      <ImageView
        android:id="@+id/btn_switch_camera"
        android:layout_width="@dimen/skin_header_btn"
        android:layout_height="@dimen/skin_header_btn"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@drawable/skin_analysis_circle_btn"
        android:contentDescription="Cambiar cámara"
        android:padding="8dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_switch_camera" />
    </RelativeLayout>

    <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="@dimen/skin_badge_row_margin_top"
      android:layout_marginHorizontal="@dimen/skin_badge_row_margin_h"
      android:gravity="center"
      android:orientation="horizontal"
      android:weightSum="3">

      <TextView
        android:id="@+id/badge_lighting"
        android:layout_width="0dp"
        android:layout_height="@dimen/skin_badge_height"
        android:layout_marginEnd="@dimen/skin_badge_gap"
        android:layout_weight="1"
        android:background="@drawable/skin_badge_pending"
        android:gravity="center"
        android:maxLines="2"
        android:paddingHorizontal="@dimen/skin_badge_padding_h"
        android:paddingVertical="@dimen/skin_badge_padding_v"
        android:text="Iluminación"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="@dimen/skin_badge_text"
        android:textStyle="bold" />

      <TextView
        android:id="@+id/badge_pose"
        android:layout_width="0dp"
        android:layout_height="@dimen/skin_badge_height"
        android:layout_marginHorizontal="@dimen/skin_badge_gap"
        android:layout_weight="1"
        android:background="@drawable/skin_badge_pending"
        android:gravity="center"
        android:maxLines="2"
        android:paddingHorizontal="@dimen/skin_badge_padding_h"
        android:paddingVertical="@dimen/skin_badge_padding_v"
        android:text="Mirada recta"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="@dimen/skin_badge_text"
        android:textStyle="bold" />

      <TextView
        android:id="@+id/badge_face"
        android:layout_width="0dp"
        android:layout_height="@dimen/skin_badge_height"
        android:layout_marginStart="@dimen/skin_badge_gap"
        android:layout_weight="1"
        android:background="@drawable/skin_badge_pending"
        android:gravity="center"
        android:maxLines="2"
        android:paddingHorizontal="@dimen/skin_badge_padding_h"
        android:paddingVertical="@dimen/skin_badge_padding_v"
        android:text="Posición de la cara"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="@dimen/skin_badge_text"
        android:textStyle="bold" />
    </LinearLayout>
  </LinearLayout>

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingBottom="28dp"
    android:paddingHorizontal="24dp">

    <!-- Solo visible si el AAR Perfect no carga (último recurso). -->
    <Button
      android:id="@+id/btn_capture"
      android:layout_width="match_parent"
      android:layout_height="48dp"
      android:backgroundTint="#6C4EE3"
      android:enabled="false"
      android:text="Capturar"
      android:textAllCaps="false"
      android:textColor="#FFFFFF"
      android:visibility="gone" />
  </LinearLayout>

  <TextView
    android:id="@+id/status_text"
    android:layout_width="0dp"
    android:layout_height="0dp"
    android:visibility="gone" />

  <TextView
    android:id="@+id/sdk_badge"
    android:layout_width="0dp"
    android:layout_height="0dp"
    android:visibility="gone" />

  <Button
    android:id="@+id/btn_cancel"
    android:layout_width="0dp"
    android:layout_height="0dp"
    android:visibility="gone" />
</FrameLayout>
