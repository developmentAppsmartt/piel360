import { AdminStub } from "@/components/admin/admin-stub";

export default function Page() {
  return (
    <AdminStub
      title="Notificaciones"
      blurb="Centro de alertas del backoffice (badge en el header)."
    />
  );
}
