import { AdminStub } from "@/components/admin/admin-stub";

export default function Page() {
  return (
    <AdminStub
      title="Compras y transacciones"
      blurb="Ledger de recargas de unidades y pagos. Mientras tanto usa Suscripciones y Gateway configs."
    />
  );
}
